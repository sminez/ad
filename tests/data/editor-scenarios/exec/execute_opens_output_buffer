Executing a command should open the corresponding output buffer.

-- config --
tests/data/config/without-fsys.toml
-- file-foo.txt --
foo
-- file-bar.txt --
bar
-- file-baz.txt --
ls
-- actions --
type: x@
# commands run async so we need to wait for the output to come through
sleep_ms: 100
-- buffer-list --
1      foo.txt
2      bar.txt
3      baz.txt
4    * +output
-- expected-windows --
3
4
