for in match
fn trait impl
return pub crate mut
& -> :: +=
normal code
"this is some text"
// this is a comment
format!("foo bar {}", baz);

&mut Bar, &mut Baz

impl Default for Foo {
    fn default() -> Self {
        Foo { life: 42 }
    }
}

const BAR: u32 = 42;

pub(crate) trait Frobnicate {
    type Frob: Ord;
}
