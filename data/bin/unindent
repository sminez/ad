#!/usr/bin/env bash
source "$HOME/.ad/lib/ad.sh"

requireAd
addr="$(bufRead "$AD_BUFID" addr | sed -E 's/(.+):.+,(.+):.+/\1,\2/')"
adEdit '. x/.+/ n/^    / d'
echo "$addr" | bufWrite "$AD_BUFID" addr
